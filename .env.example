# Bit Rot Detector — secrets file
#
# Copy this file to .env and set your SMTP credentials.
# All other configuration lives in config.yaml (see config.yaml.example).

# SMTP credentials (required for email notifications)
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password
